{
  "name": "songloft-loudness-normalizer",
  "version": "1.0.0",
  "description": "批量调整MP3响度",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --outfile=dist/index.js && cp static/index.html dist/ && chmod +x build.sh && ./build.sh"
  },
  "devDependencies": {
    "@songloft/plugin-sdk": "latest",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  }
}
